# cypressinterview

a) Clone the code into the system
b) Run npx install to install packages from package.json 
c) Run npx cypress open to open the cypress runner
d) select and run the required spec file
